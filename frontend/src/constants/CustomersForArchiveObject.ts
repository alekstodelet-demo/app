export type CustomersForArchiveObject = {
    id: number;
    full_name?: string,
    pin?: string,
    address?: string,
    is_organization?: false,
    description?: string;
    dp_outgoing_number?: string;
};