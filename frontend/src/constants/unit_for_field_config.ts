import { Dayjs } from "dayjs";

export type unit_for_field_config = {
  
  id: number;
  unitId: number;
  fieldId: number;
  // created_at: Dayjs;
  // updated_at: Dayjs;
  // created_by: number;
  // updated_by: number;
};
