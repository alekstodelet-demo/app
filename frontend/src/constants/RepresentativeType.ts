import { Dayjs } from "dayjs";

export type RepresentativeType = {
  
  id: number;
  description: string;
  name: string;
  code: string;
};


export type RepresentativeTypeCreateModel = {
  
  id: number;
  description: string;
  name: string;
  code: string;
};
