import { Dayjs } from "dayjs";

export type unit_type = {
  
  id: number;
  // created_at: Dayjs;
  // updated_at: Dayjs;
  // created_by: number;
  // updated_by: number;
  name: string;
  code: string;
  description: string;
};
