import pages from './pages';
import organizationMenu from './organization-menu';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [pages, organizationMenu]
};

export default menuItems;
